[toolchain]
# Pinned. Generator determinism (design §8) is only meaningful relative to a
# pinned toolchain, so this file is load-bearing, not hygiene.
channel = "1.97.1"
components = ["rustfmt", "clippy"]
